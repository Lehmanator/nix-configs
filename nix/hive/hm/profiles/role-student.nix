{ inputs, config, lib, pkgs, ... }:
{
  home.packages = [
    # pkgs.nur.repos.federicoschonborn.gradebook
  ]
  # https://nur.nix-community.org/repos/pokon548/
  # (with pkgs.nur.repos.pokon548; [favagtk flowtime geogebra ntfy-native sticky vaults])
  ;

}
